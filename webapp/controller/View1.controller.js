sap.ui.define(["sap/ui/core/mvc/Controller","sap/m/MessageBox"], function(Controller,MessageBox) {
	"use strict";
	return Controller.extend("com.patsDecodeEncodeBase64.controller.View1", {
		/**
		 *@memberOf com.patsDecodeEncodeBase64.controller.View1
		 */
		onEncode: function() {
			//This code was generated by the layout editor.

			var basic = this.byId("user").getValue() + ":" + this.byId("password").getValue();

			//this.byId("result").setText(btoa(basic));

			MessageBox.success("Result:" + btoa(basic));
		
			
			// //try opening dialog
			// var oView = this.getView();
			// var oDialog = oView.byId("helloDialog");
			// // create dialog lazily
			// if (!oDialog) {
			// 	// create dialog via fragment factory
			// 	oDialog = sap.ui.xmlfragment(oView.getId(), "com.patsDecodeEncodeBase64.view.Dialog", this);
			// 	oView.addDependent(oDialog);
			// }

			// oDialog.open();

		},

		onCloseDialog: function() {
			this.getView().byId("helloDialog").close();
		},
		
		onDecode: function() {
			//This code was generated by the layout editor.

			var encoded = this.byId("encoded").getValue();

			//this.byId("result1").setText(atob(encoded));
				MessageBox.success("Result:" + atob(encoded));

		}
	});
});